<!--<h1>編集画面</h1>-->
<!--%= form_with model:@infolist, url:"/infolists/#{@infolist.id}", local:true do |f| %>-->
<!--  <h4>タイトル</h4>-->
<!--  %= f.text_field :title %>-->
<!--  <h4>本文</h4>-->
<!--  %= f.text_area :body %>-->
<!--  %= link_to "削除", destroy_infolist_path(@infolist.id), method: :delete, "data-confirm" => "本当に削除しますか？" %>-->
<!--  %= f.submit '保存' %>-->
<!--% end %>-->


<div class='row'>
		<h1>編集</h1>
		<%= form_with model:@infolist,local:true do |f| %>
  <div class="field row">
    <%= f.label :title, "タイトル" %>
    <%= f.text_field :title %>
  </div>

  <div class="row">
    <%= f.label :boby, "本文" %>
    <%= f.text_area :body%>
  </div>
  
  <div class="row">
    <%= f.label :image, "画像" %>
    <%= f.attachment_field :image %>
  </div>

  <div class="row">
    <%= link_to "削除", destroy_infolist_path(@infolist.id), method: :delete, "data-confirm" => "本当に削除しますか？" %>
    <%= f.submit '保存' %>
  </div>
<% end %>

		<%= link_to '投稿詳細', infolist_path(@infolist), class: "infolist_#{@infolist.id}" %> |
		<%= link_to '戻る', infolist_path, class: "back" %>
    </div>
